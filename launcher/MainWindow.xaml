<Window x:Class="NorthstarLauncher.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:NorthstarLauncher"
        mc:Ignorable="d"
        Title="Northstar" Height="900" Width="600"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" FontSize="18" FontWeight="SemiBold" Text="Launcher" Margin="0,0,0,12" />

        <TextBox Grid.Row="1"
                 Text="{Binding ErrorMessage, Mode=OneWay}"
                 IsReadOnly="True"
                 TextWrapping="Wrap"
                 VerticalScrollBarVisibility="Auto"
                 HorizontalScrollBarVisibility="Disabled"
                 Margin="0,0,0,12" />

        <Button Grid.Row="2" Content="Close" Width="110" HorizontalAlignment="Right" Click="OnCloseClicked" />
    </Grid>
</Window>
